// Type inference = TypeScript automatically determines a value's type from the code.

/*
let age = 21;
let age : number = 21;

age -> number

let name = "suraj"; // string
let marks = 85;     // number
let passed = true;  // boolean

// Function Inference
function add(a: number, b: number) {
    return a + b;
}

// We can explicitly write:
function add(a: number, b: number): number {
    return a + b;
}

// => It reduces unnecessary code.
*/

console.log("Hello!!!");