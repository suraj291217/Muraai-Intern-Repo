/* interface Box<T> {
    value : T;
}

let numberBox: Box<number> = {
    value : 100
};

console.log(numberBox);

let stringBox: Box<string> = {
    value : "Suraj"
};

console.log(stringBox);

enum Direction {
    up, down, left, right
};

let direction: Direction = Direction.down;
console.log(direction);

enum Status {
    success = "success",
    failed = "failed",
    pending = "pending"
};

let lemon: Status = Status.pending;
console.log(lemon);
*/
